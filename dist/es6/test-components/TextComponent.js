import React from 'react';
export const TextComponent = (props) => {
    return React.createElement("span", null, props.text);
};
export const TextComponentMappedActions = (props) => {
    return React.createElement("span", null, props.text);
};
export const TextComponentWithPrefix = (props) => {
    return React.createElement("span", null, props.text);
};
export const FetchingTextComponent = (props) => {
    return React.createElement("span", null, props.text);
};
